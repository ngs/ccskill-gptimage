---
id: 013
task: gpt-image-1.5 フォールバックの動作確認(透過 PNG 用途)
date: 2026-04-23
status: 完了
phase: 0
depends_on: [002]
blocks: []
parallel_group: B
---

> ✅ **issue #014 の検証作業内で実 API 検証も完了(2026-04-23)。`gpt-image-1.5 + --background transparent --quality medium` で `generated_images/20260423_111303.png` を生成、`file` で `RGBA, with alpha` を確認。詳細: `docs/research-notes-014.md`**

## タスク概要

gpt-image-2 が透過背景非対応のため、`--model gpt-image-1.5 --background transparent` での切替経路を実 API で動作確認する。Phase 0 のリリース前に、SKILL.md / README に書いた「`gpt-image-1.5` でも対応可能」という案内を実証しておく。

## 検証手順

```bash
venv313/bin/python generate_image.py \
  "A minimalist logo of a fox, flat vector, navy and gold" \
  --model gpt-image-1.5 \
  --background transparent \
  --output-format png \
  --quality medium
```

確認項目:
- 200 OK で PNG が返る
- `file` コマンドで `RGBA, with alpha` を確認
- macOS のプレビューで透過背景になっている
- メタデータ JSON サイドカーに `model: gpt-image-1.5` が記録される

## 実装メモ

- 現状の `generate_image.py` は `--model` 引数で任意のモデル ID を受け付ける
- `args.model.startswith("gpt-image-2")` のバリデーションは 1.5 を素通りさせる仕様にしてある
- 1.5 と 2 でレスポンス形式に差異がないか確認(両方 `b64_json` のはず)
- レスポンスに `revised_prompt` が含まれるか観察

## 完了条件

- [ ] gpt-image-1.5 で透過 PNG が生成される
- [ ] 生成画像が実際にアルファチャネル付き
- [ ] 既存テスト 39 件すべて引き続き pass
- [ ] SKILL.md/ja.md の透過セクション(2. gpt-image-1.5 切替)が実証ベースで信頼できる状態に
- [ ] 必要に応じて 1.5 用のテスト 1〜2 ケース追加(モック)
